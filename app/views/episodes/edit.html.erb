<h1>Editing Episode</h1>

<%= render 'form', episode: @episode %>

<%= link_to 'Show', [@podcast, @episode], class: 'btn btn-light' %>
<%= link_to 'Back', podcast_episodes_path(@podcast), class: 'btn btn-light' %>
